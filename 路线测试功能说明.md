# 路线测试功能 · 项目文档

> 版本：1.0.0 ｜ 应用：莫酱妙妙屋（客户端 / 服务端 / 管理端）＋ BetterGI 脚本
> 核心用途：管理端发布锄地路线测试任务，用户端通过 BGI 脚本申请并执行单条路线测试，自动统计耗时 / 怪物 / 材料并上报，服务端去重归档、发放积分。

---

## 1. 功能概述

| 角色 | 职责 |
|---|---|
| **管理端** | 上传路线文件、配置路线类型 / 测试次数 / 统计模式 / 奖励积分，查看测试进度，下载测试数据 |
| **服务端** | 维护任务池与结果记录，接收用户申请并按偏好分配路线，去重归档结果、发放积分 |
| **用户端** | 配置"路线测试"调度与测试 / 兜底 BGI，申请任务、填充路线文件、启动一条龙、上报结果 |
| **BGI 脚本** | `AutoMojangRouteTest`：填写配队与路线偏好并发起申请；`RouteTest`：实际执行单条路线并生成结果指令 |

---

## 2. 整体工作流程

```
┌─ 管理端 ─────────────────────────────────────────────┐
│ 发布任务：上传路线 JSON + 类型 + 次数 + 积分等        │
└──────────────────────┬───────────────────────────────┘
                       │ publish-route-test (socket)
                       ▼
┌─ 服务端 ─────────────────────────────────────────────┐
│ route_tasks.json（任务池）                            │
│ ① 校验路线 JSON 合法性 → 生成 taskId → 存入任务池    │
└───────────────────────────────────────────────────────┘

┌─ 用户端（BGI）───────────────────────────────────────┐
│ AutoMojangRouteTest 脚本配置：                       │
│   · 路线偏好（多选：全选/战斗/食材采集/特产采集/其他） │
│   · 战斗配队名称 + 采集配队 1/2/3（名称 + 角色）      │
│ 执行 → 写 command.json（route-test-apply）           │
└──────────────────────┬───────────────────────────────┘
                       │ file-command 调度轮询触发
                       ▼
┌─ 用户端（莫酱妙妙屋）────────────────────────────────┐
│ ① 检测是否存在"路线测试"调度，无则不响应             │
│ ② 发 apply-route-test（携带偏好 + 配队信息）         │
└──────────────────────┬───────────────────────────────┘
                       │ socket
                       ▼
┌─ 服务端 ─────────────────────────────────────────────┐
│ ① 记录 userAccounts.routeTestProfile（偏好/配队）    │
│ ② 分配候选：active + 类型匹配 + 未满员               │
│     + 用户当天未测过该路线                           │
│ ③ 命中 → route-test-distribute（含路线文件内容）     │
│    未命中 → route-test-error（触发用户端兜底）       │
└──────────────────────┬───────────────────────────────┘
                       │ socket
                       ▼
┌─ 用户端（莫酱妙妙屋）────────────────────────────────┐
│ 命中：写路线文件 → 生成 OneDragon/ScriptGroup 配置    │
│       → 用【测试BGI】启动测试一条龙                  │
│ 未命中：用【兜底BGI】启动兜底一条龙（替代任务）       │
└──────────────────────┬───────────────────────────────┘
                       ▼
┌─ RouteTest 脚本（在测试 BGI 内运行）─────────────────┐
│ ① 读取 settings.routeFileName / taskId               │
│ ② 时间限制检查 → 传送神像（水下先学螃蟹技能）        │
│ ③ 跑图前统计（怪物：生物志 OCR / 材料：背包扫描）    │
│ ④ 执行单条路线 → 计时                               │
│ ⑤ 跑图后统计 → 差值 = 本次击杀/收集                  │
│ ⑥ 按 moraRate 估算摩拉 → 写 command.json            │
│    （route-test-result：耗时/怪物/材料/摩拉）        │
└──────────────────────┬───────────────────────────────┘
                       │ file-command 调度轮询触发
                       ▼
┌─ 用户端（莫酱妙妙屋）────────────────────────────────┐
│ route-test-result 指令 → 上报服务端                  │
└──────────────────────┬───────────────────────────────┘
                       │ socket
                       ▼
┌─ 服务端 ─────────────────────────────────────────────┐
│ ① 去重：accountId + 当天 + taskId 已存在则拒绝       │
│ ② 写 route_test_records.json → 累加任务 results      │
│ ③ 发放积分：account.points += task.points            │
│ ④ 满 requiredCount → 任务状态置 completed            │
└───────────────────────────────────────────────────────┘
```

---

## 3. 消息协议（socket）

| 方向 | type | 说明 |
|---|---|---|
| 管理端→服务端 | `publish-route-test` | 发布路线测试任务（含路线文件内容与参数） |
| 管理端→服务端 | `get-route-test-tasks` | 查询任务列表与进度 |
| 管理端→服务端 | `get-route-test-records` | 查询某任务结果明细 |
| 管理端→服务端 | `download-route-test-data` | 下载测试数据（可选 taskId，不传=全部） |
| 服务端→管理端 | `route-test-published` | 发布成功 |
| 服务端→管理端 | `route-test-tasks` | 任务列表（resultCount/status） |
| 服务端→管理端 | `route-test-records` | 结果明细 |
| 服务端→管理端 | `route-test-data-file` | 下载数据（fileName + content） |
| 用户端→服务端 | `get-route-test-types` | 拉取已知路线类型（备用） |
| 用户端→服务端 | `apply-route-test` | 申请任务（selectedTypes / routePreferences / combatTeamName / collectTeams） |
| 用户端→服务端 | `route-test-result` | 上报测试结果（taskId/路线/耗时/怪物/材料/摩拉） |
| 服务端→用户端 | `route-test-distribute` | 下发任务（taskId/路线文件/参数） |
| 服务端→用户端 | `route-test-error` | 申请失败或无可分配任务（含 message） |
| 服务端→用户端 | `route-test-result-accepted` | 上报成功确认 |

### apply-route-test 消息示例

```json
{
  "type": "apply-route-test",
  "selectedTypes": ["食材采集", "特产采集"],
  "routePreferences": ["食材采集", "特产采集"],
  "combatTeamName": "主力队",
  "collectTeams": [
    { "name": "材料队", "characters": ["纳西妲", "夜兰", "万叶", "钟离"] },
    { "name": "特产队", "characters": [] }
  ]
}
```

> 说明：`selectedTypes` 含 `"全选"` 时置空数组（服务端视为不限类型）；勾选"全选"等价于接受所有类型任务。

### route-test-distribute 消息示例（服务端下发）

```json
{
  "type": "route-test-distribute",
  "data": {
    "taskId": "RT-xxxx",
    "routeFileName": "202璃月-渌华池.json",
    "routeContent": { "title": "...", "description": "...", "route": [...] },
    "routeType": "食材采集",
    "statisticsMode": "识别材料",
    "activatePickup": true,
    "underwater": false,
    "timeRule": ""
  }
}
```

### route-test-result 指令（脚本写入 command.json）

```json
{
  "mojiang-command": true,
  "command": "route-test-result",
  "params": {
    "taskId": "RT-xxxx",
    "routeFileName": "202璃月-渌华池.json",
    "routeTime": 321,
    "expectMora": 1234,
    "normalNum": 3,
    "eliteNum": 1,
    "monsterNum": { "丘丘人": 5, "丘丘岩盔王": 1 },
    "itemNum": { "兽肉": 3, "禽肉": 2 }
  }
}
```

> 统计模式为"识别怪物"时上报 `monsterNum`，为"识别材料"时上报 `itemNum`；普通/精英怪数量与摩拉为估算值。

---

## 4. 服务端数据存储

| 文件 | 内容 |
|---|---|
| `data/route_tasks.json` | 任务池：`{ taskId, routeFileName, routeContent, routeType, statisticsMode, activatePickup, underwater, timeRule, requiredCount, points, results[], status(active/completed), createdAt }` |
| `data/route_test_records.json` | 结果：`{ taskId, accountId, date, routeTime, expectMora, normalNum, eliteNum, monsterNum, itemNum, reportedAt }` |
| `data/userAccounts.json` | 每个账户含 `routeTestProfile`：`{ routePreferences, selectedTypes, combatTeamName, collectTeams, updatedAt }` |

**核心规则：**
- 同一天（凌晨 4 点分界 `getTodayDateStr()`）同一用户同一路线**只能测试一次**；同一用户一天可测多条不同路线
- 多次测试由多个用户完成，或同一用户次日再次申请
- 积分 `points` 在上报且去重通过时发放（默认 0.2 分/人/次，支持小数，管理端可配置）
- 满 `requiredCount` 份有效记录后任务自动置 `completed`

---

## 5. 用户端配置指南

### 5.1 新增"路线测试"调度

任务调度 → 新增调度 → 触发器选择 **路线测试**，配置：

| 配置项 | 说明 |
|---|---|
| 监听命令文件 | 指向测试 BGI 的 `User\JsScript\RouteTest\command.json` |
| 测试BGI文件夹 | 测试路线用的 BGI 版本（可独立于主 BGI） |
| 测试一条龙配置 | 命中任务后启动的测试一条龙（由客户端动态生成） |
| 兜底BGI文件夹 | 无任务分配时启动的兜底 BGI（外部指定路径） |
| 兜底一条龙配置 | 无任务分配时启动的兜底一条龙 |

> 该调度同时承担：接收 `route-test-apply` 申请指令触发申请、接收 `route-test-result` 结果指令触发上报。

### 5.2 AutoMojangRouteTest 脚本（申请发起）

在测试 BGI 中运行 `AutoMojangRouteTest` 脚本，配置：
- **路线偏好（多选）**：全选 / 战斗 / 食材采集 / 特产采集 / 其他
- **战斗配队名称**
- **采集配队 1/2/3**：各含名称 + 角色（角色用中文逗号 `，` 分隔）

脚本执行后写 `command.json` 的 `route-test-apply` 指令，客户端检测到有"路线测试"调度才响应；无调度则忽略指令。

### 5.3 结果上报

客户端收到 `route-test-result` 指令后自动上报，无需用户操作。

---

## 6. 管理端使用指南

1. 顶部导航点击 **路线测试**
2. 发布表单：
   - 上传路线 JSON 文件（自动校验格式）
   - 选择路线类型：战斗 / 食材采集 / 特产采集 / 其他
   - 设置测试次数（≥1）、统计模式（识别怪物/识别材料）、启用拾取、水下、时间限制规则
   - 设置奖励积分（默认 0.2 分/人/次）
3. 任务列表：查看进行中/已完成状态、进度（x/requiredCount）、奖励积分，可查看结果明细、下载单任务数据 / 全部数据

---

## 7. 脚本与目录说明

### 7.1 js/AutoMojangRouteTest/（申请发起脚本）

| 文件 | 说明 |
|---|---|
| `main.js` | 读取配置 → 解析角色（中文/英文逗号）→ 写 `route-test-apply` 指令 |
| `settings.json` | 路线偏好多选（`multi-checkbox`）+ 战斗配队 + 采集配队 1/2/3 名称与角色 |
| `command.json` | 命令文件占位（`{}`） |

### 7.2 js/RouteTest/（单路线执行脚本，随客户端分发至测试 BGI）

| 文件/目录 | 说明 |
|---|---|
| `main.js` | 由 HoeingPathingTest 派生的单路线版：时间限制 → 传送 → 前后统计 → 跑图 → 写结果指令 |
| `settings.json` | `routeFileName` / `taskId` / `statisticsMode` / `activatePickup` / `underwater` / `timeRule` / `notify` |
| `pathing/` | 路线文件目录（服务端下发时由客户端写入） |
| `records/` | 本地结果备份（saved_files） |
| `assets/` | 怪物图鉴 / 怪物图鉴数字 / 材料截图 / 背包物品数字 / 学习螃蟹技能等（871 个文件，与 HoeingPathingTest 一致） |

### 7.3 客户端动态生成的一条龙配置

收到 `route-test-distribute` 后，客户端写入测试 BGI：
- `User\OneDragon\<测试一条龙配置名>.json`
- `User\ScriptGroup\<测试一条龙配置名>.json`（Javascript project，`folderName: "RouteTest"`，`jsScriptSettingsObject.routeFileName` 为下发路线名）

---

## 8. 涉及代码文件清单

| 端 | 文件 | 主要改动 |
|---|---|---|
| 服务端 | `server/server.js` | 任务池/记录数据、7 个消息 case、积分发放、去重、下载接口 |
| 管理端 | `admin/admin.html` | 路线测试导航 + 发布表单（类型下拉）+ 进度/结果/下载 |
| 用户端 | `client/index.html` | `route-test` 调度类型、申请/分发/兜底/上报处理、偏好设置 |
| 脚本 | `js/AutoMojangRouteTest/*` | 申请发起（偏好 + 配队） |
| 脚本 | `js/RouteTest/*` | 单路线执行与结果生成 |

---

## 9. 常见问题

**Q：为什么申请不响应？**
A：客户端未配置"路线测试"触发器类型的调度。脚本指令会被忽略。

**Q：为什么申请提示"没有符合所选类型的可用路线测试任务"？**
A：服务端任务池中没有同时满足 类型匹配 + 未满员 + 当天未测过 的任务；此时客户端会自动用兜底一条龙替代。

**Q：同一天能测几次？**
A：同一用户同一天同一路线只能 1 次；不同路线不受限，第二天可再次测同一路线。

**Q：积分支持小数吗？**
A：支持。积分底层为浮点累加，发布时默认 0.2 分/人/次。
